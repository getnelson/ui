import { install } from '@material-ui/styles';

/**
 * @material-ui/styles the default style implementation for the core components in Material-UI v4.
 * This installation step is temporary. Behind the scenes, the install() function switches the styling engine the core
 * components use. - Material UI team
 */
install();
